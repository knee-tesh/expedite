{
  "name": "expedite",
  "version": "1.0.0",
  "description": "this is a assignment project",
  "main": "index.js",
  "scripts": {
    "bundle-lambda": "webpack --mode=production",
    "bundle-development": "webpack --mode=development",
    "lint": "eslint .",
    "lint:quiet": "eslint . --quiet",
    "lint:fix": "eslint . --fix",
    "prettier": "prettier --check '{bin,lib,mock-data,script,pipeline,src,stage_vars,test,sam,e2e}/**/*.{js,json,md,ts,yaml,yml}' '*.{js,json,md,ts,yaml,yml}'",
    "prettier:fix": "prettier --write '{bin,lib,mock-data,script,src,pipeline,stage_vars,test,sam,e2e}/**/*.{js,json,md,ts,yaml,yml}' '*.{js,json,md,ts,yaml,yml}'",
    "test": "jest --collect-coverage --detectOpenHandles"
  },
  "author": "nitesh tiwari",
  "license": "ISC",
  "devDependencies": {
    "aws-cdk-lib": "^2.141.0",
    "constructs": "^10.3.0",
    "esbuild-loader": "^4.0.1",
    "prettier": "^3.2.5",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.4.2",
    "webpack": "^5.86.0",
    "webpack-cli": "5.1.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.363.0",
    "uuid": "^8.2.0"
  }
}
